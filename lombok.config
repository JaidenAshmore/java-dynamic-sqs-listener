lombok.addLombokGeneratedAnnotation = true
# Adds @ConstructorProperties to constructors so that Jackson Deserialization is able to deserialize the fields
lombok.anyConstructor.addConstructorProperties=true